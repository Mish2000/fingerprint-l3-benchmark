# Development coordinator. Model frameworks live in their own worker profiles.
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.13.15=*_cp313
  - numpy=2.5.2
  - pytest=9.1.1
  - ruff=0.16.5
  - pip=26.2.1
